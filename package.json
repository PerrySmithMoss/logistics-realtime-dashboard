{
  "name": "logistics-realtime-dashboard",
  "private": true,
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "prepare": "husky",
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "validate": "pnpm --filter server exec lint-staged && pnpm --filter client exec lint-staged"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
